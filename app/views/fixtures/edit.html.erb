<% provide(:form_title, 'Edit Fixture') %>
<% provide(:form_url, '/fixtures/' + @fixture.id.to_s) %>


<div id="edit-fixture" class="admin-only admin-form">
  <div class="admin-navbar">
  </div>
  <%= render 'shared/flash' %>
  <section class="form">
    <%= render 'fixture_form' %>
  </section>
</div>
