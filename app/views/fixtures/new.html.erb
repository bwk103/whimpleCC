<% provide(:form_title, 'Add Fixture') %>
<% provide(:form_url, '/fixtures') %>

<div id="new-fixture" class="admin-only admin-form">
  <div class="admin-navbar">
  </div>
  <%= render 'shared/flash' %>
  <section class="form">
    <%= render 'fixture_form' %>
  </section>
</div>
