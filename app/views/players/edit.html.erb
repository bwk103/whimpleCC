<% provide(:form_title, 'Edit Player') %>
<% provide(:form_url, '/players/' + @player.id.to_s) %>


<div id="new-player" class="admin-only admin-form">
  <div class="admin-navbar">
  </div>
  <%= render 'shared/flash' %>
  <section class="form">
    <%= render 'player_form' %>
  </section>
</div>
